# Python built in functions and libraries
# common built-in functions: len(), type(), input(), print
# exploring standard libraries:math,random


import math
print(math.sqrt(16))
import random
print(random.radiant(1, 10))